# 模板抽屉快捷键集成总结

## 本轮目标
- 模板抽屉按钮 tooltip 需要显示快捷键。
- 快捷键需要和现有任务列表、Markdown 抽屉开关保持一致。

## 已实施
- 新增 `shortcuts.openTemplates = "⌘⇧L"`。
- 模板按钮 tooltip 现在显示 `⌘⇧L`。
- 全局键盘处理新增 `⌘⇧L`，用于打开或关闭模板抽屉。

## 未解决
- 暂无阻断问题。

## 文档判断
- README 当前不维护快捷键列表，本次不更新 README。
