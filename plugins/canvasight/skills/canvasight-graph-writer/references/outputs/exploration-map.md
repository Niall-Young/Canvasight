# Output: exploration-map

Use a central problem or intent node with distinct branches for confirmed context, assumptions, questions, constraints, and candidate directions.

- Keep each branch semantically consistent.
- Promote independently explorable assumptions, questions, evidence, and directions from long branch prose into connected nodes; keep only content that jointly frames one inquiry together.
- Connect evidence and questions to the direction they affect.
- Place unresolved directions side by side; do not imply a sequence unless one exists.
- Prefer a horizontal dependency layout with the central intent at the left.
